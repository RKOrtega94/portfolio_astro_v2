---
import MainLayout from "../../presentation/layouts/main.astro";
---

<MainLayout>
  <section class="w-full h-full flex justify-center items-center gap-2 p-4">
    <aside class="w-full max-w-lg flex flex-col gap-2">
      <div class="w-full flex gap-2">
        <input
          type="text"
          class="w-full rounded p-2 border-none bg-gray-100 ring-0 outline-none border-b-2 border-gray-300 focus:border-blue-500 text-black"
          placeholder="Nombre"
        />
        <input
          type="text"
          class="w-full rounded p-2 border-none bg-gray-100 ring-0 outline-none border-b-2 border-gray-300 focus:border-blue-500 text-black"
          placeholder="Teléfono"
        />
      </div>
      <input
        type="text"
        class="w-full rounded p-2 border-none bg-gray-100 ring-0 outline-none border-b-2 border-gray-300 focus:border-blue-500 text-black"
        placeholder="Correo"
      />
    </aside>
  </section>
</MainLayout>
